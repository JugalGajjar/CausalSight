# conda env create -f env/environment.yml
# Python 3.11: the ML stack does not yet support the system's Python 3.14.
name: causalsight
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - ffmpeg
  - git
  - pip:
      - -r requirements.txt
